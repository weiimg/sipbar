# 安全性

## 這個程式碰得到什麼

Sipbar 是一支只在本機執行的 Windows 桌面程式。判斷任何回報是不是安全性問題
之前，先看它實際的範圍：

| | |
|---|---|
| 網路 | **沒有**。程式不連線、不上傳、不下載。唯一會離開程式的動作是叫**你的瀏覽器**去開一個網址（引導裡的喝水歌、設定頁的「回報問題」），Sipbar 自己不送任何東西 |
| 帳號、密碼、金鑰 | 沒有 |
| 讀寫範圍 | `%LOCALAPPDATA%\Sipbar\`，以及開機自啟的那一筆登錄值（`HKCU\Software\Microsoft\Windows\CurrentVersion\Run`）|
| 會解析的外來資料 | 只有你自己在設定頁挑的那個 `.wav` |

所以沒有伺服器可以打、沒有憑證會外洩、沒有資料會離開這台電腦。能寫進
`%LOCALAPPDATA%\Sipbar\` 的人，本來就已經是你這個 Windows 帳號了。

## 怎麼回報

到 [GitHub Issues](https://github.com/weiimg/sipbar/issues) 開一則。

這是個人專案，沒有專門的資安信箱。如果你認為某個問題**公開描述會傷到使用者**，
在 issue 裡先只寫「有安全性問題想私下說明」，我會回覆聯絡方式。

回報時附上設定頁「關於」區塊的**診斷資訊**（那顆「複製」）。那段字裡沒有你的
喝水紀錄、沒有體重、也沒有你的 Windows 帳號名稱——家目錄會被換成
`%USERPROFILE%`。

## 支援範圍

只有**最新的一個版本**。這個程式是攜帶版，更新就是解壓縮覆蓋，沒有升級路徑
需要維護，所以舊版不回頭修。

## 兩件已知、而且不打算在近期改的事

**執行檔沒有數位簽章。** 憑證要年費，而這是免費的個人專案。後果是 Windows
SmartScreen 會對每個下載的人跳警告。折衷做法是每個 release 都附上
`Sipbar-x.y.z-portable.zip` 的 SHA256，你可以自己驗：

```powershell
Get-FileHash .\Sipbar-0.10.0-beta-portable.zip -Algorithm SHA256
```

**Qt 是凍在發布包裡的。** 攜帶版夾帶整套 Qt，你自己更新不了。Qt 有安全性修正
時，只能等這裡重建再發一次。目前使用的 Qt 版本寫在診斷資訊裡，
所以「我拿到的是哪一版」是查得出來的。
