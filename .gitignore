# 使用者的個人設定，絕對不能進版本庫。
#
# settings.py 的 _migrate_legacy() 會把「程式旁邊的 config.json」當成舊版設定，
# 搬進使用者的 %LOCALAPPDATA%\Sipbar\。所以只要這個檔案跟著發布出去，
# 每個新使用者一裝就會繼承開發者的個人設定（體重推算出的目標、提醒間隔…），
# 而且完全沒有任何提示。
config.json

__pycache__/
*.py[cod]

# 測試產出的圖與假資料
tests/*.png
tests/wp_*/
tests/*.json
tests/*.jsonl

build/
dist/
*.spec

# 開發日誌。裡面有體重、作息與工作習慣的觀察，還有給 AI 的交接註記，
# 那些不該公開。要引用它的結論，把結論寫進 docs/ 底下對應的檔案。
notes/

# 社群用的 GIF。同一份內容的 WebP 只有 1/10 大而且圓角乾淨，那個進版本庫就夠了。
# GIF 是二值透明，注定有鋸齒，留著只是為了貼在不吃 WebP 的平台上——
# 要用的時候跑 `python tools/make_social_demo.py` 就有，不必背在 git 歷史裡。
docs/social-laptop.gif
